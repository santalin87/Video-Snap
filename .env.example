# VidSnap 环境变量配置
# 复制此文件为 .env 并填写对应值

# ─────────── 后端（VPS 上设置）───────────

# CORS 允许的来源（逗号分隔）
# 包含 GitHub Pages 地址 和 你的域名
ALLOWED_ORIGINS=https://santalin87.github.io,https://api.yourdomain.com

# yt-dlp 代理（可选，留空则不使用）
# YTDLP_PROXY=socks5://127.0.0.1:1080

# ─────────── 前端（GitHub Secrets 中设置）───────────
# 在 GitHub Repo → Settings → Secrets and variables → Actions 中添加：
# Secret 名: VITE_API_BASE_URL
# Secret 值: https://api.yourdomain.com  （你在 Cloudflare 配置的 API 域名）

# ─────────── 本地开发 ───────────
# 本地开发无需设置 VITE_API_BASE_URL，Vite 会自动代理到 localhost:8000
